# Copy this file to .env (gitignored) and set a real value before running:
#   docker compose up
#
# Must satisfy SQL Server's password policy (8+ chars, upper+lower+digit or symbol).
MSSQL_SA_PASSWORD=Your_strong_password123
